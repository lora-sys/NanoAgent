# NanoAgent CLI 界面使用指南

## 🎯 概述

NanoAgent 现在配备了一个简洁的 CLI 界面，用于实时交互和监控。

## 📋 特性

### 1. 实时编排器
- 显示 Agent 执行阶段
- 实时显示思考过程
- 展示工具调用和结果
- 进度条显示

### 2. HITL 交互
当 Agent 需要人类输入时，CLI 界面会：
- **向用户提问** - 获取用户偏好或澄清
- **决策审批** - 展示分析和建议，等待审批
- **干预请求** - 在错误场景下请求人类介入
- **任务升级** - 超出能力范围时升级给人类
- **反馈收集** - 收集人类反馈用于学习
- **监控通知** - 日志审查和实时监控

## 🚀 使用方式

### 基础使用

```python
from core.agent_loop import NanoAgent

# 创建 Agent（自动初始化 CLI）
agent = NanoAgent()

# 运行任务
result = agent.run("创建一个 FastAPI 登录模块")
```

### CLI 界面会自动显示

```
============================================================
🔄 Planning Phase (Step 1)
============================================================

💭 Thinking: 正在分析任务需求...

🔧 Executing: write_file with filepath='main.py'

✅ Successfully wrote 150 chars to main.py
------------------------------------------------------------

[████████████░░░░░░░░░] 3/5 (60%) 正在处理中...

============================================================
🎉 任务完成
============================================================

任务完成：创建了 5 个文件，测试通过
```

### HITL 交互示例

当 Agent 需要用户输入时：

```
============================================================
🤔 Agent 需要你的帮助：
============================================================

问题：你希望使用哪个数据库？

可选答案：
  1. PostgreSQL
  2. MySQL
  3. SQLite
  4. MongoDB

============================================================

请输入你的回答：[1/2/3/4 或自定义] > 
```

决策审批场景：

```
============================================================
⚠️  需要人类审批的决策
============================================================

分析：需要删除测试数据库中的所有数据

建议：执行数据库清理操作

风险：medium

============================================================

批准？[y/n] > 
```

## 🎨 界面元素

### 阶段标识
- `🔄 Planning Phase` - 规划阶段
- `🔄 Think Phase` - 思考阶段
- `🔄 Observe Phase` - 观察阶段
- `🔄 Reflection Phase` - 反思阶段

### 状态图标
- `💭` - 思考中
- `🔧` - 工具执行
- `📝` - 内容输出
- `✅` - 成功
- `❌` - 错误
- `⚡` - 实时监控
- `🚨` - 干预请求
- `🎉` - 完成

### 进度条
```
[████████████░░░░░░░░░] 3/5 (60%) 正在处理中...
```

## 💡 最佳实践

1. **资源节省** - CLI 界面最小化开销，不影响 Agent 性能
2. **实时反馈** - 所有操作立即显示，提供完整可见性
3. **简洁交互** - 只在需要时请求输入，避免打扰
4. **清晰输出** - 使用图标和格式化，提高可读性

## 🔧 自定义

如果你需要自定义 CLI 行为，可以继承 `CLIInterface`：

```python
from cli_interface import CLIInterface

class MyCLI(CLIInterface):
    def display_result(self, result: str, success: bool = True):
        # 自定义结果显示
        super().display_result(result, success)
        if success:
            print(f"📊 操作耗时: {self.get_execution_time()}s")

# 使用自定义 CLI
from cli_interface import set_cli_interface
set_cli_interface(MyCLI())
```

## 📊 监控和日志

所有交互都会记录到日志文件：
- `nanoagent.log` - 主日志文件
- `agent_workspace/human_feedback.jsonl` - 人类反馈记录

## 🚨 注意事项

1. CLI 界面会自动初始化，无需手动设置
2. 在非交互环境中（如 CI/CD），HITL 工具会自动跳过
3. 所有用户输入都是可选的，Agent 可以继续执行
4. 反馈数据会持久化存储，用于后续优化

## 🎯 总结

CLI 界面提供：
- ✅ 实时编排能力
- ✅ 简洁的交互方式
- ✅ 完整的 HITL 支持
- ✅ 资源高效利用
- ✅ 清晰的视觉反馈

让每个部分都是实时编排器，最大化任务效果！